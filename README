Support for Arguments
-f (file path)
-w (word count)
-l (lines)
-b (bytes)
-k (kilobytes)
-m (megabytes)
-g (gigabytes)

This Application is a `wc` clone using Multi_processing
Run using `python word_vectorizer.py [-f file_path] [-<other flags>]`
